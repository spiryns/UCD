// ═══════════════════════════════════════════════════════
// Kopieer dit bestand naar  secrets.h  (zonder .example) en vul je
// eigen WiFi in. secrets.h staat in .gitignore en wordt NOOIT gecommit.
// Nodig zodat de XIAO en je PC op hetzelfde netwerk zitten voor OTA.
// ═══════════════════════════════════════════════════════
#pragma once

#define WIFI_SSID     "JOUW_WIFI_NAAM"
#define WIFI_PASSWORD "JOUW_WIFI_WACHTWOORD"

// mDNS-hostname; hiermee flash je draadloos via env:handle_ota.
#define OTA_HOSTNAME  "sensepath-handle"
